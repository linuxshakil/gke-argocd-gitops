environment: test

preSyncCheck:
  enabled: true

image:
  tag: "REPLACE_ME_VIA_PROMOTE_WORKFLOW" # never edit this by hand — run promote.yml instead

replicaCount: 2

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 70

ingress:
  enabled: true
  host: "test.myahad.online"
  className: "gce"
